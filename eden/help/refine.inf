
********************************************************

Run this program by typing

	eden [-v]  refine  pname

where pname stands for an input parameter file name
without extension '.inp'

     optional -v (verbose) flag produces a real space
     R function listing in pname.rsr;
     default (no -v) produces no pname.rsr.

For more information, type 

	eden -h refine

********************************************************

